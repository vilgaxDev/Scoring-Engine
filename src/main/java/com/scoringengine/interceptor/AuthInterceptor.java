package com.scoringengine.interceptor;
/*

import kotlin.jvm.internal.Intrinsics;
import okhttp3.Interceptor;
import okhttp3.Request;
import okhttp3.Response;
import org.jetbrains.annotations.NotNull;
import org.springframework.lang.NonNull;

import java.io.IOException;


public class AuthInterceptor implements Interceptor {
    public final String mAuthToken;

    @NotNull
    public Response intercept(@NonNull @NotNull Interceptor.Chain chain) throws IOException {
        Intrinsics.checkNotNullParameter(chain, "chain");
        Request request = chain.request().newBuilder().addHeader("Authorization", "Bearer " + this.mAuthToken).build();
        Response var10000 = chain.proceed(request);
        Intrinsics.checkNotNullExpressionValue(var10000, "chain.proceed(request)");
        return var10000;
    }

    public AuthInterceptor(@NotNull String mAuthToken) {
        super();
        Intrinsics.checkNotNullParameter(mAuthToken, "mAuthToken");

        this.mAuthToken = mAuthToken;
    }
}

 */
